{% import '@SyliusAdmin/Macro/breadcrumb.html.twig' as breadcrumb %}

{% set vars = configuration.parameters.get('vars') %}

{% set breadcrumbs = [
        { label: 'sylius.ui.administration'|trans, url: path('sylius_admin_dashboard') },
        { label: 'asdoria.ui.pictogram_groups'|trans , url: path('asdoria_admin_pictogram_group_index')},
        { label:  vars.pictogramGroup.name , url: path('asdoria_admin_pictogram_group_update', { 'id': vars.pictogramGroup.id })},
        { label: (metadata.applicationName~'.ui.'~metadata.pluralName)|trans },
    ]
%}

{{ breadcrumb.crumble(breadcrumbs) }}
